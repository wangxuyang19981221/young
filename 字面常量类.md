# 聚合类

- 所有的成员都是public的
- 没有virtual函数，基类
- 没有定义任何构造函数
- 没有类内初始值


# 字面常量类
- 数据成员类型均为字面值类型的聚合类
如果类不是聚合类，那么满足以下条件则也是字面常量类
- 至少有一个constexpr构造
- 所有的数据成员必须是字面值类型
- 必须使用默认的析构定义
- 如果数据成员含有类内初始值，则初始值必须是常量表达式，如果数据成员是类类型，则必须使用其自己的constexpr构造